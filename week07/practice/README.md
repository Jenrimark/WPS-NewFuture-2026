此目录存放课堂作业（week07）。

- `01_nginx_volume`：包含
  - **21.Docker 最小化实例**（Go Web 服务 + Docker 最小镜像）
  - **22.Nginx 容器与本地目录映射**（`-v` 挂载 + `-p` 端口映射 + 热更新）
- `02_go_api`：**23.Go 后端 API 的容器化构建**（Gin + 多阶段构建 + 最终镜像 < 20MB）