# 提交规范（Commit Message Convention）

本项目的 Git 提交信息统一遵循 **Conventional Commits** 风格，目标是：

- 让提交历史可读、可搜索
- 让变更类型清晰（便于生成变更日志、回滚、代码审查）
- 让 AI/自动化工具可以稳定地解析与生成提交信息

---

## 1. 标题（Subject）格式（必须）

**统一格式：**

```
<type>(<scope>): <subject>
```

其中：

- **type**：变更类型（必填）
- **scope**：影响范围（可选，但推荐填；不用时省略括号）
- **subject**：一句话说明本次变更做了什么（必填）

**标题要求：**

- 使用现在时、祈使句风格（例如“添加/修复/更新/移除”，不要写“已添加/已修复”）
- 只写“做了什么”，不要展开“怎么做”（细节放正文）
- 建议不超过 72 个字符（中文可适当放宽，但尽量简洁）
- 不在末尾加句号

---

## 2. type 取值（必须）

优先使用以下集合（尽量不要自造新 type）：

- **feat**：新增功能（面向用户/业务能力的新增）
- **fix**：修复缺陷（bug 修复）
- **docs**：文档变更（仅文档、注释、说明）
- **style**：代码样式调整（不影响逻辑；格式化、空格、分号等）
- **refactor**：重构（非新增功能、非修 bug 的代码调整）
- **perf**：性能优化
- **test**：测试相关（新增/调整测试）
- **build**：构建系统或依赖变更（构建脚本、依赖版本等）
- **ci**：CI 配置变更
- **chore**：杂项（不属于以上类别，如工具配置、清理、临时脚本等）
- **revert**：回滚某次提交（见“回滚”章节）

> 提示：如果你在纠结 `refactor` vs `feat/fix`，看“对用户/外部行为”是否改变：改变就用 `feat/fix`，否则多半是 `refactor`。

---

## 3. scope 规则（推荐）

**scope 用来标注影响范围**，便于筛选历史。建议取值：

- 模块/目录：`api`、`ui`、`auth`、`build`、`docs`
- 具体功能域：`login`、`payment`、`report`
- 基础设施：`deps`、`release`、`config`

**scope 写法：**

- 使用小写英文/数字/短横线：`user-profile`
- 只写一个最主要的范围；如果确实影响多个范围，尽量拆分提交

---

## 4. 正文（Body）（可选但推荐）

当标题不足以说明“为什么/影响/取舍”时，添加正文。

**正文建议包含：**

- **为什么要改**（问题背景/需求）
- **改了什么**（高层描述）
- **影响与风险**（兼容性、回滚点）

**格式：**

- 标题与正文之间空一行
- 正文可以多段落；每段之间空一行

---

## 5. 脚注（Footer）（可选）

用于关联 issue/任务、注明破坏性变更等：

- 关联 issue：`Refs #123`、`Closes #123`
- 破坏性变更：`BREAKING CHANGE: ...`（见下一节）

---

## 6. 破坏性变更（Breaking Change）

当变更会导致调用方/用户需要调整用法（接口不兼容、配置迁移等），必须显式标记。

**两种写法（二选一或都写）：**

1) 在标题 type 后加 `!`：

```
feat(api)!: 移除 v1 旧接口
```

2) 在 Footer 添加 `BREAKING CHANGE:`：

```
BREAKING CHANGE: v1 接口已移除，请改用 /v2/...
```

---

## 7. 回滚（revert）

回滚提交使用 `revert`，并尽量注明被回滚的提交：

```
revert(<scope>): <subject>
```

正文建议包含：

- `This reverts commit <hash>.`
- 回滚原因（可选）

---

## 8. 示例（可直接套用）

**新增功能：**

```
feat(auth): 支持短信验证码登录
```

**修复缺陷：**

```
fix(ui): 修复暗色模式下按钮文字不可见
```

**重构：**

```
refactor(api): 拆分用户查询逻辑以降低复杂度
```

**性能优化：**

```
perf(report): 优化导出查询减少 30% 响应时间
```

**文档：**

```
docs: 补充本地开发与发布流程
```

**破坏性变更（标题带 !）：**

```
feat(config)!: 重命名环境变量前缀为 APP_
```

**带正文与脚注：**

```
fix(payment): 修复重复扣款在网络抖动下的重试问题

原因：支付回调超时后会触发上游重试，旧逻辑未做幂等校验。
方案：在扣款前增加幂等键校验，并对重复请求返回已处理结果。

Closes #248
```

---

## 9. AI 生成提交信息的固定规则（给 AI 用）

当 AI 需要生成提交信息时，必须遵循以下步骤：

1) **先判断类型**：优先在 `feat/fix/refactor/docs/test/build/ci/chore/perf/style` 中选择最贴近的一个  
2) **选择 scope**：用最主要的模块名/目录名/功能域；不确定时可省略 scope  
3) **写 subject**：一句话描述“做了什么”，不要堆砌细节，不要写“更新代码/修复问题”这种泛化表述  
4) **如有必要写正文**：当变更动机、影响、风险不明显时补充  
5) **如有破坏性变更**：必须加 `!` 或 `BREAKING CHANGE:`（至少一个）  
6) **如关联任务**：在 Footer 用 `Refs #id` 或 `Closes #id`  

**可复制模板：**

```
<type>(<scope>): <subject>

<body: why + what + impact>

<footer: Refs/Closes #id>
```

